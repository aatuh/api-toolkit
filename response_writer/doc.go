// Package response_writer provides legacy JSON response helpers retained for v2
// source compatibility.
//
// New code should prefer github.com/aatuh/api-toolkit/v2/httpx for JSON success
// responses, Problem Details errors, response capture, and response wrapper
// behavior. This package is stable for v2 callers but compatibility-sensitive
// and not a template for new response helper design.
package response_writer
