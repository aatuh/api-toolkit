// Package migrate provides the migrate CLI.
//
// The CLI surfaces unresolved started or uncertain migration states as
// operator-visible errors instead of retrying them automatically.
package migrate
