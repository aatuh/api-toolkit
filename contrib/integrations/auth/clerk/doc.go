// Package clerk provides convenience wiring around the Clerk auth middleware.
//
// The package aliases the underlying middleware types and exposes environment
// loading helpers for quick application bootstrap. Import the middleware package
// directly when you need the narrowest dependency surface.
package clerk
