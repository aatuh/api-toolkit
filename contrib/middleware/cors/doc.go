// Package cors provides contrib CORS middleware configuration.
//
// Use explicit allowed origins, methods, and headers for browser-facing APIs.
// Avoid wildcard origins in production unless the route is intentionally public
// and credential-free.
package cors
