apiVersion: v1
kind: Service
metadata:
  name: api-admin
  annotations:
    api-toolkit.dev/internal-only: "true"
spec:
  type: ClusterIP
  selector:
    app: api
  ports:
    - name: admin
      port: 9090
      targetPort: admin
